# Learnings

Human-reviewed log of recurring spec/harness fixes. The skill appends an entry
only when a perturbed-test failure forces a change, and only after the user
approves the entry. The skill never modifies itself autonomously.

Format per entry:

```
## YYYY-MM-DD — <short title>
- Context: <domain/task and what perturbed input broke it>
- Fix: <the spec or harness change made>
- Generalizable lesson: <what to watch for in future spec extraction>
```

<!-- No entries yet. -->
